<section id="main" class="<%= stats.attr("total") > 0 ? "show" : "" %>">
	<input id="toggle-all" type="checkbox" <%= stats.attr("allCompleted") ? "checked" : "" %>>
	<label for="toggle-all">Mark all as complete</label>
	<ul id="todo-list"></ul>
</section>